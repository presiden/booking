package com.okestudio.booking.service;

import java.time.LocalDate;
import java.util.List;
import java.util.Optional;
import com.okestudio.booking.entity.Shows;
import com.okestudio.booking.entity.Film;
import com.okestudio.booking.entity.Theater;
import com.okestudio.booking.entity.TheaterRoom;

public interface ShowsService {
    // Shows save(Shows show);
    // List<Shows> findAll();
    // Optional<Shows> findById(Long id);
    // Shows update(Shows show);
    // void deleteById(Long id);
    // List<Shows> findByDate(LocalDate date);
    // List<Shows> findByFilm(Film film);
    // List<Shows> findByTheater(Theater theater);
    // List<Shows> findByTheaterRoom(TheaterRoom theaterRoom);
    // List<Shows> findByDateAndTheater(LocalDate date, Theater theater);
}