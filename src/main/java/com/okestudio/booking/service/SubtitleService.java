package com.okestudio.booking.service;

import java.util.List;
import java.util.Optional;
import com.okestudio.booking.entity.Subtitle;

public interface SubtitleService {
    // Subtitle save(Subtitle subtitle);
    // List<Subtitle> findAll();
    // Optional<Subtitle> findById(Long id);
    // Subtitle update(Subtitle subtitle);
    // void deleteById(Long id);
    // Subtitle findByName(String name);
    // Subtitle findByCode(String code);
    // List<Subtitle> findByIsActive(Boolean isActive);
}